# 图片和视频存储修复说明

## 问题描述

之前系统直接将火山引擎API返回的临时URL存储到数据库中。这些临时URL有过期时间（通常24小时），重启服务器后URL可能已失效，导致图片和视频无法访问。

## 解决方案

系统已更新，现在会自动将图片和视频下载到本地存储，并使用本地URL替换临时URL。

### 主要改动

1. **文件存储服务** (`backend/app/services/file_storage.py`)
   - 自动下载图片和视频到本地
   - 使用MD5哈希值作为文件名，避免重复下载
   - 支持按项目组织文件（可选）

2. **图片生成服务更新**
   - 生成图片后立即下载并保存到本地
   - 使用本地URL替换临时URL

3. **视频生成服务更新**
   - 视频生成完成后立即下载并保存到本地
   - 使用本地URL替换临时URL

4. **静态文件服务**
   - 在 `main.py` 和 `main_prod.py` 中添加了静态文件服务
   - 本地文件可通过 `/static/media/` 路径访问

### 存储目录结构

```
backend/
  storage/
    images/
      project_1/  (可选，按项目组织)
        img_xxx.png
      img_xxx.png
    videos/
      project_1/  (可选，按项目组织)
        video_xxx.mp4
      video_xxx.mp4
```

### 配置说明

在 `backend/config.py` 中新增了以下配置：

- `STORAGE_DIR`: 文件存储目录（默认: `./storage`）
- `IMAGES_DIR`: 图片存储目录（默认: `./storage/images`）
- `VIDEOS_DIR`: 视频存储目录（默认: `./storage/videos`）
- `STATIC_URL_PREFIX`: 静态文件URL前缀（默认: `/static/media`）

可以通过环境变量覆盖这些配置：

```bash
export STORAGE_DIR="./custom_storage"
export STATIC_URL_PREFIX="/media"
```

## 使用说明

### 新生成的图片和视频

新生成的图片和视频会自动下载到本地，无需额外操作。

### 已存在的旧数据

对于数据库中已存在的旧数据（使用临时URL），有以下几种处理方式：

#### 方式1：重新生成（推荐）

如果项目还在编辑中，可以重新生成图片和视频，系统会自动保存到本地。

#### 方式2：手动迁移（适用于重要数据）

如果需要保留旧数据，可以：

1. 检查数据库中的URL是否仍然有效
2. 如果有效，可以编写脚本批量下载并更新URL
3. 如果已失效，只能重新生成

#### 方式3：等待自然更新

当用户重新生成图片或视频时，系统会自动使用本地存储。

## 注意事项

1. **存储空间**：图片和视频文件会占用本地磁盘空间，请确保有足够的存储空间
2. **备份**：建议定期备份 `storage/` 目录
3. **清理**：可以定期清理不再使用的文件（需要手动实现清理逻辑）
4. **性能**：首次下载会增加生成时间，但后续访问会更快

## 故障排查

### 问题：图片/视频仍然无法显示

1. 检查 `storage/` 目录是否存在且有文件
2. 检查静态文件服务是否正确挂载（查看启动日志）
3. 检查文件权限
4. 检查URL路径是否正确

### 问题：下载失败

1. 检查网络连接
2. 检查原始URL是否仍然有效
3. 查看日志文件了解详细错误信息

### 问题：存储空间不足

1. 清理旧的、不再使用的文件
2. 考虑使用外部存储服务（如OSS、S3等）
3. 调整存储策略（如压缩、定期清理等）

## 未来改进建议

1. **自动清理**：实现自动清理不再使用的文件
2. **外部存储**：支持OSS、S3等云存储服务
3. **CDN加速**：使用CDN加速文件访问
4. **压缩优化**：自动压缩图片和视频以节省空间
